{
  "name": "interface-concept-checker",
  "version": "0.1.0",
  "description": "Deterministic engine for the Interface Seam Finder. Pure logic, no DB, no LLM, no frontend.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "tsx": "^4.22.4",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
